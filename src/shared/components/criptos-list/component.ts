import { Component, OnInit, ViewChild, ElementRef } from "@angular/core";
@Component({
    templateUrl: "template.html",
    selector: "cripto-list",
    styleUrls: ["style.scss"]
})

export class ComponentCriptoList implements OnInit {
    constructor(
    ) {
    }
    async ngOnInit(){
    }
    
}